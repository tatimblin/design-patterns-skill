# Weather Dashboard Example

A mini app that pulls weather data from two external APIs with incompatible response formats. The dashboard normalizes each response inline before rendering a combined forecast.

# Shape Editor Example

A mini canvas app where shapes can be duplicated from pre-configured templates. Each shape type knows how to copy itself, and a registry holds named templates that users can stamp onto the canvas at different positions.

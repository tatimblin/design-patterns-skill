# User Profile Example

A mini app where `UserProfile` objects are constructed with many optional fields. Creating a profile with only a few fields filled in requires passing `null` placeholders for the rest.

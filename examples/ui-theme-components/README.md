# UI Theme Components Example

A mini app with standalone factory functions for creating themed UI widgets — buttons, checkboxes, and modals. Each function independently handles theme selection, and nothing prevents mixing components from different themes in a single view.

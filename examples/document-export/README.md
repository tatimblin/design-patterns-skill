# Document Export Example

A mini app where `DocumentExporter` generates PDF, HTML, and Markdown output. Each format requires assembling a structurally different artifact — headers, body wrapping, and metadata differ per format.
